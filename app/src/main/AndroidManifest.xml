<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.attendence">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/applogo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/applogo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AddStudentActivity"></activity>
        <activity android:name=".UpdateTeacherActivity" />
        <activity android:name=".DeleteTeacherActivity" />
        <activity android:name=".AddTeacherActivity" />
        <activity android:name=".NextActivityOfTakeAttendence" />
        <activity android:name=".ViewAttendenceActivity" />
        <activity android:name=".TakeAttendenceActivity" />
        <activity android:name=".AChoiceActivity" />
        <activity android:name=".TChoiceActivity" />
        <activity android:name=".AdminActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>